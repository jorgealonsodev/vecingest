# Go
/api/vecingest
/api/*.test
/api/coverage.out

# Node / pnpm
node_modules/
.turbo/
dist/
.expo/

# Env
.env
.env.local
deploy/.env

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Local agent tooling — the skill registry holds absolute paths from one machine
.atl/

# CodeGraph index — local to this checkout. Its root path and the indexed bytes
# are checkout-specific, so every worktree needs its own; never share or copy one.
.codegraph/
.playwright-mcp/
