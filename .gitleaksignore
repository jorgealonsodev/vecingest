# Both fingerprints below are confirmed false positives from switching the
# `gitleaks` job to a full-history scan (`gitleaks detect --source .`,
# see .github/workflows/security.yml). Format: commit:file:rule-id:line.
#
# Neither description below repeats the flagged wording/value verbatim --
# doing so would itself read as a high-entropy string and get flagged on
# THIS file (already hit once while drafting this).

# generic-api-key false positive: prose inside a gosec-job explanatory
# comment in security.yml discussing golangci-lint suppression syntax.
# Natural-language wording, not a credential.
6b229b776497521c08809807cfeebc3a4e0efb70:.github/workflows/security.yml:generic-api-key:64

# generic-api-key false positive: a hardcoded fixture constant used only
# by api_integration_test.go's own HTTP test server, never a real
# credential.
6b229b776497521c08809807cfeebc3a4e0efb70:api/internal/http/api/api_integration_test.go:generic-api-key:35

# generic-api-key false positive, already committed and immutable: an
# earlier revision of this very file (commit 2cb437e) quoted the flagged
# workflow-comment wording above verbatim while explaining it, which
# re-triggered the same rule on THIS file. The wording was reworded in a
# later commit so no new instance of this recurs; this entry only
# silences the one historical occurrence.
2cb437ededb88a97f69e8a0ba85fb967f70f8a58:.gitleaksignore:generic-api-key:6
