# M0 Security Gate — Base

PRD_go.md section 10.1, "M0 — Base": twelve verifiable checklist items.
Per PRD section 10: "La puerta se verifica con evidencia (resultado de CI,
informe, captura, entrada en el runbook), no con una afirmación." — the
gate is verified with evidence, never a bare assertion.

Split into two checkpoints so a green CI run is never mistaken for a
closed milestone (`openspec/changes/m0-foundation/tasks.md`, Phase 13/14):

- **Checkpoint A** — closeable entirely from this repository (code, tests,
  CI runs, documents). No deployed server is required.
- **Checkpoint B** — requires the target server deployed under Portainer.
  Tracked separately; **in progress as of 2026-09-08** — the server exists
  and most of it is verified, but the PRD's own login functional criterion
  and part of gate item 6 remain open behind a superadmin account that
  does not exist yet.

Each row cites the spec requirement that carries the acceptance scenarios,
rather than restating the checklist, so the two cannot drift
(`openspec/config.yaml` rule: "the specs already cite every gate item by
number").

## Checkpoint A — code and CI evidence

| # | PRD 10.1 item | Spec requirement(s) | Evidence |
|---|---|---|---|
| 1 | Argon2id (m=19456 KiB, t=2, p=1) with a test rejecting passwords below the applicable floor (15 chars without 2FA, 12 with TOTP) or breached (HIBP) | `auth-credentials/spec.md`: "Conditional Password Length Floor", "HIBP k-Anonymity Breach Check", "Argon2id Password Hashing" | `cd api && go test -race ./...` — `internal/domain/auth/password` and `internal/platform/hibp` packages pass (see the run archived below, 2026-09-08). HIBP fail-open is an accepted residual risk, see `docs/security/threat-model.md`. |
| 2 | Refresh, invitation and reset tokens hashed in the database; refresh reuse invalidates the family | `auth-session-tokens/spec.md`: "Refresh Rotation with Family Invalidation", "Hashed Storage of Sensitive Tokens" | `cd api && go test -race ./...` — the `test/` e2e package (login → refresh rotation → reuse ⇒ family revoked) passes. |
| 3 | `Strict` + path-scoped cookie; `Origin` and CSRF token validated on `/auth/refresh` | `auth-csrf-origin/spec.md`: "CSRF Token Delivered In-Body, Never By Cookie", "Origin Validation on Refresh", "Refresh Cookie Scope and Flags" | `cd api && go test -race ./...` — the `test/` e2e package's CSRF/Origin cases (foreign `Origin` → 403, valid cookie without token → 403) pass. |
| 4 | TOTP 2FA working and mandatory for `superadmin`; superadmin login on a separate route | `auth-mfa-totp/spec.md`: "Mandatory TOTP for Superadmin on a Separate Route" | `cd api && go test -race ./...` — `internal/domain/auth/mfa` and the e2e superadmin-route case pass. |
| 5 | `internal/config` validates the environment; the process refuses to start with a missing variable | `platform-bootstrap/spec.md`: "Config Startup Validation" | `cd api && go test -race ./...` — `internal/config` per-subcommand startup tests (D-I's table) pass; see task 5.33/5.34 and Phase 1.1/1.2. |
| 7 | `gitleaks`, `govulncheck`, `gosec`, `pnpm audit --audit-level=high`, Trivy and Semgrep in CI and blocking | `platform-bootstrap/spec.md`: "CI Security Scanning Gate" | `.github/workflows/security.yml` (all six scanners, no `continue-on-error`, weekly re-scan). Local pre-flight of every scanner against this exact repository state on 2026-09-08 (archived below): govulncheck, gosec (scoped `golangci-lint --enable-only=gosec`), Semgrep, gitleaks and Trivy fs all exit 0; `pnpm audit --audit-level=high` exits 0 with the two unfixable `image-size` CVEs recorded as an accepted residual risk. The govulncheck run additionally caught and this batch fixed a real vulnerable transitive dependency (`moby/go-archive` GO-2026-6253, bumped to v0.3.0) — see "deliberately-failing probe" below. **Correction (remediation batch 2026-09-08, verify-report.md CRITICAL-4):** `security.yml`'s semgrep step previously ran only `--config p/owasp-top-ten --config p/golang --config p/typescript`, never `api/.semgrep`'s three project rules (D-O single-writer `audit_log`, the SQL/Go float-for-money ban, D-J's PII-call-site rule) — they existed but were never executed. Fixed by adding `--config api/.semgrep`; verified by planting one violation of each rule in a throwaway fixture tree outside this repository (a `float64` money field, a `double precision` migration column, a direct non-`audit.Append` call to `InsertAuditLog`, and a `slog.Info(fmt.Sprintf(...), ...)` PII call site) — all three rule IDs fired, then the fixture was deleted and the real repository re-scanned clean (`Ran 170 rules on 186 files: 0 findings`) with the exact command `security.yml` now runs. One pre-existing defect surfaced in that proof and is **not** fixed here (`api/.semgrep` is outside this batch's file scope): `no-interpolated-pii-in-slog-message`'s `slog.$LEVEL(fmt.Sprintf(...), ...)` pattern requires at least one argument *after* the formatted message (the trailing `, ...)`) and does not match the simplest violation shape, a bare `slog.Info(fmt.Sprintf("...", pii))` with no further arguments — confirmed by isolating the pattern against both shapes. Needs a `pattern-either` covering both the zero-trailing-arg and the with-trailing-args call shapes. **Real pipeline run confirmed (2026-09-08):** the local pre-flight above is no longer the only evidence — `gh run view` against `main` `HEAD` `6c0cb8d` shows a real, green `security.yml` run (34276511534: semgrep, govulncheck, pnpm-audit, gitleaks, gosec, trivy, all passing with non-trivial durations) and a real, green `ci.yml` run (34276511537). Verbatim output in `docs/security/evidence/checkpoint-b/2026-09-08-server-verification.md` §1 (task 13.2). |
| 9 | `app_rw` has no `UPDATE`/`DELETE`/`TRUNCATE` on append-only tables (`audit_log`, and `votes`/`time_entries` when they exist), is not their owner nor a member of the owner role | `db-access-control/spec.md`: "Runtime Role Append-Only Restriction" | `cd api && go test -race ./...` — the Testcontainers privilege-matrix integration test passes (both `PARTITION OF` and `ATTACH PARTITION` cases, D-B). |
| 10 | Rate limiting (`httprate`) with `trust proxy` scoped to `PROXY_IP` | `request-protection/spec.md`: "Trusted-Proxy Client IP Resolution" | **Corrected citation (remediation batch 2026-09-08, verify-report.md CRITICAL-3):** the row previously cited "the spoofed-`X-Forwarded-For`-from-an-untrusted-source table test" — no such test existed; the only test on file (`TestClientIPStep_SpoofedLeftmostEntryIgnored`) sets `RemoteAddr` to the *trusted* proxy, so it never exercised an untrusted peer. That gap is now closed: `internal/http/router/router_test.go` — `TestPeerTrustGuard_UntrustedPeerRejectsForgedXFF` sets `RemoteAddr` to an untrusted peer (`198.51.100.7`) with a forged `X-Forwarded-For: 1.2.3.4` and asserts the resolved client IP is the peer's real address, not the forged header; `TestSteps_OrderMatchesD_H` asserts the new `peer-trust-guard` step precedes `client-ip` in the actual middleware chain. Re-run and confirmed passing during this remediation batch: `cd api && go test -race -count=1 -run 'TestPeerTrustGuard_UntrustedPeerRejectsForgedXFF|TestSteps_OrderMatchesD_H' ./internal/http/router/...` → `PASS`. |
| 11 | PII redaction in the `slog` handler, tested against a log containing an email, phone and IBAN | `request-protection/spec.md`: "slog PII Redaction" | `cd api && go test -race ./...` — `internal/observability/slogpii` passes. |
| 12 | Initial threat model written (`docs/security/threat-model.md`) from PRD table 6.1 | `platform-bootstrap/spec.md`: "Initial Threat Model Document" | `docs/security/threat-model.md` (this batch), covering every PRD 6.1 threat row and three accepted residual risks. |

### Archived evidence (2026-09-08)

```
$ cd api && go test -race ./... -count=1
ok  	.../cmd/lintcompose	1.023s
ok  	.../cmd/openapi-gen	1.022s
ok  	.../cmd/vecingest	62.694s
ok  	.../internal/config	1.021s
ok  	.../internal/config/secrets	1.012s
ok  	.../internal/db	16.925s
ok  	.../internal/domain/audit	31.885s
ok  	.../internal/domain/auth/lockout	1.014s
ok  	.../internal/domain/auth/mfa	46.102s
ok  	.../internal/domain/auth/password	1.760s
ok  	.../internal/domain/auth/session	47.890s
ok  	.../internal/domain/auth/token	1.019s
ok  	.../internal/domain/pii	1.017s
ok  	.../internal/health	10.273s
ok  	.../internal/http/api	70.763s
ok  	.../internal/http/handlers	1.014s
ok  	.../internal/http/router	1.011s
ok  	.../internal/observability/slogpii	1.016s
ok  	.../internal/platform/attempts	1.015s
ok  	.../internal/platform/cache	1.015s
ok  	.../internal/platform/hibp	1.020s
ok  	.../internal/platform/limiter	1.015s
ok  	.../internal/platform/migrate	29.490s
ok  	.../internal/platform/queue	21.651s
ok  	.../migrations/schema	38.776s
ok  	.../test	57.803s
(27/27 packages ok)

$ ~/go/bin/golangci-lint run ./...
0 issues.

$ pnpm --filter app test
Test Suites: 4 passed, 4 total
Tests:       10 passed, 10 total

$ pnpm turbo run lint typecheck test   # ci.yml's ts job, all 4 workspace packages
Tasks: 9 successful, 9 total

$ cd api && DOCKER_HOST=unix:///nonexistent-docker.sock go test -race -short -count=1 ./...
(27/27 packages ok, ~4.8s -- proves the fast unit job's skippability claim
with no reachable Docker daemon, the load-bearing detail ci.yml's `go` job
depends on)

$ cd api && ~/go/bin/govulncheck ./...
No vulnerabilities found. (after bumping github.com/moby/go-archive
v0.2.0 -> v0.3.0; see "deliberately-failing probe" below)

$ cd api && ~/go/bin/golangci-lint run --enable-only=gosec ./...
0 issues.   # matches security.yml's gosec job exactly

$ semgrep scan --config p/owasp-top-ten --config p/golang --config p/typescript --error
Ran 165 rules on 173 files: 0 findings.

$ docker run --rm -v "$(pwd):/repo" zricethezav/gitleaks:latest git /repo --no-banner
no leaks found

$ docker run --rm -v "$(pwd):/repo" -w /repo aquasec/trivy:latest fs --severity HIGH,CRITICAL --exit-code 1 /repo
0 vulnerabilities across api/go.mod and pnpm-lock.yaml (2 unfixable
image-size CVEs excluded via .trivyignore; accepted risk, see threat-model.md)

$ pnpm audit --audit-level=high
4 vulnerabilities found, Severity: 2 moderate | 2 high (2 ignored)   # exit 0
```

### Task 11.11 — the deliberately-failing probe

A live GitHub Actions run of a probe PR could not be produced from this
offline batch (no `git push`/PR creation available in this session). The
equivalent, and stronger, evidence: running every `security.yml` scanner
locally against this repository's **real** dependency tree, before any fix,
genuinely failed —

```
$ cd api && ~/go/bin/govulncheck ./...
Vulnerability #1: GO-2026-6253
    moby/go-archive: Crafted tar archive can write outside the extraction
    directory in github.com/moby/go-archive
    Found in: github.com/moby/go-archive@v0.2.0
    Fixed in: github.com/moby/go-archive@v0.3.0
Your code is affected by 1 vulnerability from 1 module.
```

reached through `internal/testhelpers` (a real, exploitable transitive
dependency, not a synthetic probe), which is exactly the failure
`security.yml`'s `govulncheck` job is wired to block on. This batch fixed it
(`go get github.com/moby/go-archive@v0.3.0`) and re-ran clean, demonstrating
the full red -> green cycle the gate is meant to enforce. `security.yml`
itself has not yet executed on GitHub's infrastructure — that first real run
is Checkpoint B/deployment-adjacent evidence, not fabricated here.

### Cross-check (task 13.5)

Every Checkpoint A row above cites either a passing local test command or an
archived document, never a bare assertion, per PRD section 10's own rule.

## Checkpoint B — deployed-server evidence (IN PROGRESS — 2026-09-08)

The infrastructure blocker is lifted: a real Portainer host exists at
`vecingest.xdev.es` / `app.vecingest.xdev.es` / `api.vecingest.xdev.es`, all
five containers (`vecingest-{api,worker,db,web,site}-1`) are `Up`/`(healthy)`,
and every check below was run read-only against that live host via SSH.
Verbatim command output for everything in this section is archived in
`docs/security/evidence/checkpoint-b/2026-09-08-server-verification.md`.
**A green Checkpoint A still does not close M0 by itself** — the PRD's own
functional criterion for M0 ("Login desde web y móvil contra el servidor
desplegado por Portainer") and half of gate item 6 remain open below,
blocked on a superadmin account that does not exist yet
(`openspec/changes/m0-foundation/tasks.md` tasks 14.3/14.4/14.5).

| # | PRD 10.1 item | Spec requirement(s) | Status |
|---|---|---|---|
| 6 | Measured resource budget: `api`/`worker` < 128 MB at rest, image < 30 MB, boot < 1 s, p95 of `GET /v1/me` < 50 ms **on the server** | `user-profile/spec.md`: "GET /v1/me Performance Budget"; `platform-bootstrap/spec.md`: "Resource and Container Hardening Budget" | **PARTIAL, not green.** Measured on the real server: image sizes 7.0 MiB (`api`)/27.2 MiB (`web`)/24.7 MiB (`site`), all under the 30 MiB budget; `docker stats --no-stream` shows `api` at 4.5 MiB and `worker` at 6.0 MiB at rest, far under 128 MiB. **The p95-latency half is not measured**: `k6` against `GET /v1/me` needs an authenticated bearer token, and no user account exists on this deployment yet (`vecingest bootstrap-superadmin` has never been run). This half of the row stays open until a human creates an account and this task is re-run (task 14.5). |
| 8 | Containers `read_only`, non-root, `cap_drop: ALL`, `mem_limit`/`GOMEMLIMIT`; Portainer 2FA required; `docker.sock` not exposed to another container | `platform-bootstrap/spec.md`: "Resource and Container Hardening Budget" | **GREEN for everything CLI-verifiable.** `docker inspect` on the live server confirms `api`/`worker`: `read_only=true`, user `65532:65532`, `cap_drop=[ALL]`, `no-new-privileges:true`, `mem_limit=256m`, `GOMEMLIMIT=200MiB`; `web`/`site`: same flags, user `1000:1000`, `mem_limit=128m` (no `GOMEMLIMIT` — expected, they're nginx not Go). `db` intentionally carries none of `read_only`/`cap_drop`/non-root — this is the **documented, deliberate exception** in `docker-compose.yml`'s own comment (the official `postgres` image needs a writable data directory and its own privilege-drop entrypoint), not a gap. `docker.sock` exposure checked across **all 16 containers on the host**, not just vecingest's: only `portainer` itself mounts it. **Portainer's 2FA requirement is the one sub-item still open**: there is no CLI/API surface to read it without an interactive login this session does not hold credentials for; a human must confirm it in the Portainer UI (Settings → Authentication → 2FA) before this row can be called fully green. |
| — | PRD M0 functional criterion: login from web and mobile against the Portainer-deployed server | (functional criterion, not a numbered gate item) | **NOT STARTED.** Requires the stack actually running under Portainer (now true) AND a superadmin/user account to log in with (not yet true — nobody has run `vecingest bootstrap-superadmin`). `openspec/changes/m0-foundation/tasks.md` tasks 14.3/14.4 stay open until a human runs that command and performs both manual logins. |

Row 8 is effectively green except for the Portainer-2FA sub-check, which is
explicitly called out rather than assumed. Row 6 and the functional
criterion remain open behind the same missing account, not behind
infrastructure — the infrastructure blocker itself is resolved.
